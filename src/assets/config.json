{
	"enable-private-server": {
		"type": "boolean",
		"title": "Enable http repo server",
		"description": "Allow requesting repos from a private http server",
		"default": false
	},

	"enable-file-loading": {
		"type": "boolean",
		"title": "Enable file loading",
		"description": "Allow loading files from the local filesystem",
		"default": false
	},

	"private-server-url": {
		"type": "string",
		"title": "Private server url",
		"description": "Url for the private server",
		"default": ""
	},

	"auto-update": {
		"type": "boolean",
		"title": "Auto Update",
		"description": "Automaticaly update the modules",
		"default": false
	},

	"reinstall": {
		"type": "boolean",
		"title": "Reinstall",
		"description": "Reinstalls itself in the next page refresh (erasing all saved config)",
		"default": false
	}
}
